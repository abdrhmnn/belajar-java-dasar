package Operasi;

public class OperasiBoolean {
  // untuk dua data boolean
  // &&, ||, !

  // && (AND)
  // true && true = true
  // true && false = false
  // false && true = false
  // false && false = false
  
  // || (OR)
  // true && true = true
  // true && false = true
  // false && true = true
  // false && false = false
  
  // ! (NEGASI)
  // !true = false
  // !false = true
}
